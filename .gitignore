# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# TypeScript build artefacts (compiled JS lives alongside .ts in v0 — adjust when bundler arrives)
*.tsbuildinfo

# Coverage
coverage/
.nyc_output/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Test runner caches
.vitest/

# Sonar local cache (in case it gets scanned later)
.scannerwork/
